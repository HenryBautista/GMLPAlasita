<Page
    x:Class="Alasita.App.Views.Main"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Alasita.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:Alasita.App.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <Flyout x:Name="FlyOutViewItem" Placement="Full">
            <StackPanel Width="400">
                <TextBlock Text="{Binding Title}"
                           Style="{StaticResource TitleTextBlockStyle}"
                           Margin="10,5"
                           HorizontalAlignment="Left"
                           Width="380"/>
            </StackPanel>
            <Flyout.FlyoutPresenterStyle>
                <Style TargetType="FlyoutPresenter">
                    <Setter Property="ScrollViewer.HorizontalScrollMode" Value="Disabled"/>
                    <Setter Property="Width" Value="310"/>
                </Style>
            </Flyout.FlyoutPresenterStyle>
        </Flyout>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="8*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <!--<controls:ShareFacebook/> -->
        <ScrollViewer Grid.Row="1" Grid.Column="1" ZoomMode="Enabled" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image Source="ms-appx:///Images/feria.png" ></Image>
                <AppBarButton Label="A" Margin="396,436,0,0"></AppBarButton>
                <AppBarButton Label="B" Margin="288,341,0,0"></AppBarButton>
                <AppBarButton Label="C" Margin="103,211,0,0"></AppBarButton>
                <AppBarButton Label="D" Margin="261,207,0,0" Width="75"></AppBarButton>
                <AppBarButton Label="E" Margin="310,133,0,0"></AppBarButton>
                <AppBarButton Label="F" Margin="310,196,0,0"></AppBarButton>
                <AppBarButton Label="G" Margin="442,267,0,0"></AppBarButton>
                <AppBarButton Label="H" Margin="414,170,0,0"></AppBarButton>
                <AppBarButton Label="I" Margin="244,133,0,0"></AppBarButton>
                <AppBarButton Label="N" Margin="112,118,0,0"></AppBarButton>
                <AppBarButton Label="R" Margin="436,107,0,0"></AppBarButton>
                <AppBarButton Label="K" Margin="720,296,0,0"></AppBarButton>
                
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>

